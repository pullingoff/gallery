import React from "react";
import styled from "styled-components";

const Header = () => {
    return (
        <StyledHeader>PHOTOTYPE</StyledHeader>
    )
}

const StyledHeader = styled.header`
font-weight: bold;
font-size: 3rem;
`

export default Header;